= simple_form_for(@offer, url: [:admin, @vendor, @offer]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :image
    = f.input :name
    = f.input :description
    = f.input :link

  .form-actions
    = f.button :submit
